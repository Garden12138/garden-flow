{
  "name": "gardenflow-xhs-publisher-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "pnpm build && pnpm verify && pnpm test"
  }
}
